DATABASE_URL=file:/home/z/my-project/db/custom.db

# ── Email (Gmail SMTP) ──────────────────────────────────────────────────────
# Used to send OGI result emails to users + internal notifications to AVYSTRA.
# These credentials are server-side only — never imported in client code.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=info.avystra@gmail.com
SMTP_PASS=hmubmwuchzlcdwrj
SMTP_FROM=AVYSTRA <info.avystra@gmail.com>

# Inbox that receives the full OGI submission data (internal notifications).
AVYSTRA_NOTIFY_EMAIL=info.avystra@gmail.com
